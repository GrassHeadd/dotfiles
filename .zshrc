export DIDERO_WORK_EMAIL="jjh@didero.ai"

# Added by Didero Onboarding
export DIDERO_ROOT_PATH="~/didero"
export PATH="$HOME/.local/bin:$PATH"

# fnm (Fast Node Manager)
eval "$(fnm env --use-on-cd)"
